const initialState = {
    authToken: null,
    refreshGrid: false,
    inputParams: {
        loanSize: '',
        creditScore: '',
        propertyType: 'SingleFamily',
        occupancy: 'Primary'
    }
};

export default initialState;